import os
import sys
import json

def add_numbers(a, b):
    return a + b

def get_user(name)
    return {"name": name, "active": True}

def process_list(items):
        result = []
        for item in items:
            result.append(item)
        return result
